/**
 * Static Phrases — כל המשפטים הקבועים שיועלו לימות באתחול
 *
 * הפונקציה מחזירה מערך של כל המשפטים הסטטיים + ערכי מילון
 * שצריכים להיות מוכנים כ-MP3 בימות לפני שמתקשר ראשון מצלצל.
 */

const { numberToHebrew } = require('./tts');

function getAllStaticPhrases() {
    const phrases = new Set();

    // ==========================================
    // ניווט וממשק
    // ==========================================
    const nav = [
        'חוזרים לתפריט הראשי.',
        'מעבר להצעות חדשות.',
        'מעבר לפניות שהגיעו אליך.',
        'מעבר לסטטוס הפניות שלך.',
        'מעבר לניהול תמונות.',
        'מעבר להודעות חשובות.',
        'מעבר להגדרות.',
        'עוברים להודעה הבאה.',
        'עוברים לפנייה הבאה.',
        'לתמיכה ולעזרה, יש להיכנס לאתר פינקס.',
    ];
    nav.forEach(p => phrases.add(p));

    // ==========================================
    // הודעות מערכת
    // ==========================================
    const system = [
        'מספר הטלפון אינו רשום במערכת הפנקס. להרשמה, יש להיכנס לאתר פינקס.',
        'הגישה חסומה לשלושים דקות, עקב ניסיונות כושלים חוזרים. נסה שוב מאוחר יותר.',
        'הכניסה נחסמה זמנית עקב ניסיונות שגויים. נסה שוב מאוחר יותר. חוזרים לתפריט הראשי.',
        'קוד שגוי יותר מדי פעמים. חוזרים לתפריט הראשי.',
        'אירעה תקלה בעדכון הקוד. נסה שוב מאוחר יותר. חוזרים לתפריט הראשי.',
        'אירעה תקלה. חוזרים לתפריט הראשי.',
        'אירעה תקלה בשליחת הפנייה. עוברים להצעה הבאה.',
        'אירעה תקלה. עוברים לפנייה הבאה.',
        'אירעה תקלה. עוברים לבקשה הבאה.',
        'אין פעילות חדשה כרגע.',
    ];
    system.forEach(p => phrases.add(p));

    // ==========================================
    // סטטוסים (חסום / ממתין לאישור) — זכר ונקבה
    // ==========================================
    const statusMsg = [
        'החשבון שלך חסום. לפרטים, פנה לצוות התמיכה דרך האתר.',
        'החשבון שלך חסום. לפרטים, פני לצוות התמיכה דרך האתר.',
        'הפרופיל שלך עדיין ממתין לאישור. נעדכן אותך במייל כאשר יאושר.',
        'הפרופיל שלך עדיין ממתינה לאישור. נעדכן אותך במייל כאשר תאושרי.',
    ];
    statusMsg.forEach(p => phrases.add(p));

    // ==========================================
    // PIN — זכר ונקבה
    // ==========================================
    const pin = [
        'הזן את קוד הכניסה שלך, ארבע ספרות.',
        'הזיני את קוד הכניסה שלך, ארבע ספרות.',
        'קוד שגוי. אנא נסה שוב.',
        'קוד שגוי. אנא נסי שוב.',
    ];
    pin.forEach(p => phrases.add(p));

    // ==========================================
    // סיום רשימות
    // ==========================================
    const endList = [
        'סיימנו את כל ההצעות החדשות. חוזרים לתפריט הראשי.',
        'אין פניות ממתינות לתשובתך. חוזרים לתפריט הראשי.',
        'אין הודעות חדשות הדורשות תשומת לבך. חוזרים לתפריט הראשי.',
        'אין בקשות תמונה ממתינות. חוזרים לתפריט הראשי.',
        'אין פניות פעילות שיצאו ממך. חוזרים לתפריט הראשי.',
    ];
    endList.forEach(p => phrases.add(p));

    // ==========================================
    // פעולות — זכר ונקבה
    // ==========================================
    const actions = [
        'פנייתך נקלטה בהצלחה. עוברים להצעה הבאה.',
        'הצעה הוסרה. עוברים להצעה הבאה.',
        'דולגים להצעה הבאה.',
        'דולגות להצעה הבאה.',
        'הפנייה אושרה. עוברים לפנייה הבאה.',
        'הפנייה נדחתה. עוברים לפנייה הבאה.',
        'דולגים לפנייה הבאה.',
        'דולגות לפנייה הבאה.',
        'הסכמת לחשיפת תמונתך. עוברים לבקשה הבאה.',
        'הבקשה נדחתה. עוברים לבקשה הבאה.',
        'עוברים לבקשה הבאה.',
        'הפנייה בוטלה. עוברים לפנייה הבאה.',
        'הקוד עודכן בהצלחה. חוזרים לתפריט הראשי.',
        'אין פרטים נוספים.',
        'אין תיאור נוסף.',
        'פרטים לא זמינים.',
    ];
    actions.forEach(p => phrases.add(p));

    // ==========================================
    // תפריט הצעות — מקש לא מוכר + אפשרויות — זכר ונקבה
    // ==========================================
    const matchActions = [
        'מקש לא מוכר. הקש אחת — מעוניין. הקש שתיים — לא מעוניין. הקש שמונה — דלג.',
        'מקש לא מוכר. הקשי אחת — מעוניינת. הקשי שתיים — לא מעוניינת. הקשי שמונה — דלגי.',
        'מקש לא מוכר. הקש אחת — מסכים. הקש שתיים — לא מסכים. הקש שמונה — דחה לאוחר יותר.',
        'מקש לא מוכר. הקשי אחת — מסכימה. הקשי שתיים — לא מסכימה. הקשי שמונה — דחי לאוחר יותר.',
        'מקש לא מוכר. הקש אחת — הסכמה. הקש שתיים — דחייה. הקש שמונה — דלג.',
        'מקש לא מוכר. הקשי אחת — הסכמה. הקשי שתיים — דחייה. הקשי שמונה — דלגי.',
        'מקש לא מוכר. הקש אחת להאזנה חוזרת. הקש שמונה להודעה הבאה.',
        'מקש לא מוכר. הקשי אחת להאזנה חוזרת. הקשי שמונה להודעה הבאה.',
        'מקש לא מוכר. הקש אחת לביטול. הקש שמונה להמשך. הקש סולמית לתפריט הראשי.',
        'מקש לא מוכר. הקשי אחת לביטול. הקשי שמונה להמשך. הקשי סולמית לתפריט הראשי.',
        'מקש לא מוכר. הקש שמונה להמשך. הקש סולמית לתפריט הראשי.',
        'מקש לא מוכר. הקשי שמונה להמשך. הקשי סולמית לתפריט הראשי.',
    ];
    matchActions.forEach(p => phrases.add(p));

    // ==========================================
    // הגדרות (PIN) — זכר ונקבה
    // ==========================================
    const settings = [
        'להחלפת קוד הכניסה, הקש את הקוד הנוכחי, ארבע ספרות.',
        'להחלפת קוד הכניסה, הקשי את הקוד הנוכחי, ארבע ספרות.',
        'אנא הקש את קוד הכניסה הנוכחי.',
        'אנא הקשי את קוד הכניסה הנוכחי.',
        'קוד נכון. הקש קוד חדש בן ארבע ספרות.',
        'קוד נכון. הקשי קוד חדש בן ארבע ספרות.',
        'קוד שגוי. נסה שוב. הקש את קוד הכניסה הנוכחי.',
        'קוד שגוי. נסי שוב. הקשי את קוד הכניסה הנוכחי.',
        'הקש קוד חדש בן ארבע ספרות.',
        'הקשי קוד חדש בן ארבע ספרות.',
        'קוד לא תקין. הקש ארבע ספרות בדיוק.',
        'קוד לא תקין. הקשי ארבע ספרות בדיוק.',
        'הקש שוב את הקוד החדש לאישור.',
        'הקשי שוב את הקוד החדש לאישור.',
        'הקודים אינם תואמים. הקש קוד חדש בן ארבע ספרות.',
        'הקודים אינם תואמים. הקשי קוד חדש בן ארבע ספרות.',
    ];
    settings.forEach(p => phrases.add(p));

    // ==========================================
    // מספרים 0–99 + גילאים נפוצים 18–70
    // ==========================================
    for (let i = 0; i <= 99; i++) {
        const heb = numberToHebrew(i);
        if (heb) phrases.add(heb);
    }

    // גבהים נפוצים
    for (let h = 140; h <= 200; h++) {
        phrases.add(`גובה ${numberToHebrew(h)} סנטימטר`);
    }

    // גילאים
    for (let age = 18; age <= 70; age++) {
        phrases.add(`גיל ${numberToHebrew(age)}`);
        phrases.add(`${numberToHebrew(age)} שנים`);
    }

    // ==========================================
    // ערכי phonetic-map (מילון סטטי)
    // ==========================================
    try {
        const pm = require('./phonetic-map.json');
        const skipSections = ['_comment', 'ivr_ui', 'numbers_spoken', 'cities_phonetic', 'yeshivot_phonetic'];

        for (const [section, values] of Object.entries(pm)) {
            if (skipSections.includes(section)) continue;
            if (typeof values !== 'object' || Array.isArray(values)) continue;

            for (const [key, hebrewText] of Object.entries(values)) {
                if (key.startsWith('_')) continue;
                if (hebrewText && hebrewText.length > 0) {
                    phrases.add(hebrewText);
                }
            }
        }

        // ערים
        if (pm.cities_phonetic) {
            for (const [city, phonetic] of Object.entries(pm.cities_phonetic)) {
                if (city.startsWith('_')) continue;
                phrases.add(`מ${city}`);
            }
        }
    } catch (err) {
        console.warn('[StaticPhrases] ⚠️ שגיאה בטעינת phonetic-map:', err.message);
    }

    // ==========================================
    // ביטויים נוספים שמופיעים בבניית כרטיסיות
    // ==========================================
    const cardParts = [
        'הצעה.',
        'פנייה שהגיעה אליך.',
        'הודעה חדשה:',
        'מוצא',
        'רקע',
        'גובה',
        'סנטימטר',
        'מבנה גוף',
        'מראה',
        'גוון עור',
        'שאיפה:',
        'תחום עבודה:',
        'כיסוי ראש:',
        'בוגרת סמינר',
        'ישיבת',
        'רווק', 'רווקה',
        'גרוש', 'גרושה',
        'אלמן', 'אלמנה',
        'מבקש לראות את תמונתך.',
        'מבקשת לראות את תמונתך.',
        'טרם נענתה.',
        'שניכם הביעו עניין ראשוני.',
        'בטיפול השדכנית. אין צורך בפעולה נוספת כרגע.',
    ];
    cardParts.forEach(p => phrases.add(p));

    return [...phrases];
}

module.exports = { getAllStaticPhrases };
